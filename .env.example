# Environment configuration for the VPC Provisioning API
# Copy this file to .env and fill in the values

# AWS region where resources will be deployed
AWS_REGION=us-east-1

# CDK environment account and region (optional; will auto-detect if omitted)
# CDK_DEFAULT_ACCOUNT=
# CDK_DEFAULT_REGION=us-east-1
